[package]
name = "bevy_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.17", features = ["jpeg", "png"] }
bevy_obj = "0.17"
rand = "0.8"

[profile.release]
opt-level = "s"
codegen-units = 1
lto = "thin"
strip = true
